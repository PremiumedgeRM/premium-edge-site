import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Premium Edge Curbing - Professional Concrete Landscape Curbing',
  description: 'Transform your landscape with professional concrete curbing services. We offer residential and commercial curbing solutions to enhance your property\'s value and curb appeal.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  );
} 